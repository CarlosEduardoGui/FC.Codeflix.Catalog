version: '3.4'

services:
  fc.codeflix.catalog.db:
    container_name: catalogdb
    hostname: catalogdb
    image: mysql
    environment:
      MYSQL_TCP_PORT: 3307
      MYSQL_ROOT_PASSWORD: codeflix
    ports:
      - 3307:3307   

networks:
  default:
    name: service_catalog  